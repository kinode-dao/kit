# Kino Files UI

## development
```sh
# get your kinode running
./target/release/kinode --rpc  wss://eth-sepolia.g.alchemy.com/v2/abc123 ./your/kinode --testnet 

# in a new terminal
yarn
yarn dev

# in a new terminal
npx tailwindcss -i ./src/input.css -o ./src/index.css --watch
```